# Only the `replay` script runs in this workspace; the jobs process reads these from the api's
# .env. The pglite dir is relative to this workspace, so this is the same repo-root .data.
DATABASE_URL=pglite://../../.data
# fatal | error | warn | info | debug | trace | silent
LOG_LEVEL=info
