# Only the `generate` and `migrate` scripts run in this workspace; the api and jobs processes
# read DATABASE_URL from their own .env. The pglite dir is relative to this workspace, so this
# is the same repo-root .data the api uses.
DATABASE_URL=pglite://../../.data
